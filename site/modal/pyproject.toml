[project]
name = "character-replacement-modal"
version = "0.1.0"
description = "Modal functions for the character replacement web service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "modal>=1.3.5",
]
